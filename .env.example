# QMS Environment Configuration
# Copy this file to .env and customize the values

# Production mode (set to True for production)
PRODUCTION=True

# Security settings
# Generate a secure random key with: python3 -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=replace_with_a_secure_random_string

# Admin credentials
ADMIN_PASSWORD=replace_with_a_secure_admin_password

# Database settings (optional - defaults to SQLite)
# DATABASE_URL=postgresql://username:password@localhost/qms_db

# Timezone settings (optional - defaults to IST)
# TZ=Asia/Kolkata
